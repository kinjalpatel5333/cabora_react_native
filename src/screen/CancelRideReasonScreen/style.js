import {StyleSheet} from 'react-native';

export default function createStyles(colors) {
  return StyleSheet.create({
    root: {
      flex: 1,
      justifyContent: 'flex-end',
      backgroundColor: 'transparent',
    },
    backdrop: {
      ...StyleSheet.absoluteFillObject,
      backgroundColor: colors.alpha.navy40,
    },
    sheet: {
      backgroundColor: colors.surface,
      borderTopLeftRadius: 28,
      borderTopRightRadius: 28,
      paddingTop: 10,
      paddingHorizontal: 20,
      maxHeight: '92%',
    },
    sheetContent: {
      paddingBottom: 8,
    },
    handle: {
      alignSelf: 'center',
      width: 40,
      height: 4,
      borderRadius: 2,
      backgroundColor: colors.isDark ? colors.navy[700] : colors.gray[200],
      marginBottom: 16,
    },
    title: {
      fontFamily: colors.fonts.sora.extraBold,
      color: colors.text,
      fontSize: 22,
      marginBottom: 8,
    },
    subtitle: {
      fontFamily: colors.fonts.sora.regular,
      color: colors.textMuted,
      fontSize: 14,
      lineHeight: 20,
      marginBottom: 16,
    },
    penaltyBox: {
      flexDirection: 'row',
      alignItems: 'flex-start',
      gap: 10,
      backgroundColor: colors.isDark ? colors.alpha.red18 : colors.red[100],
      borderRadius: 16,
      borderWidth: 1,
      borderColor: colors.isDark ? colors.alpha.red40 : colors.red[200],
      paddingVertical: 12,
      paddingHorizontal: 12,
      marginBottom: 18,
    },
    penaltyIcon: {
      marginTop: 1,
    },
    penaltyCopy: {
      flex: 1,
      minWidth: 0,
    },
    penaltyTitle: {
      fontFamily: colors.fonts.sora.bold,
      color: colors.isDark ? colors.red[400] : colors.red[500],
      fontSize: 14,
      marginBottom: 4,
    },
    penaltyMeta: {
      fontFamily: colors.fonts.sora.regular,
      color: colors.isDark ? colors.red[300] : colors.red[700],
      fontSize: 12,
      lineHeight: 17,
    },
    reasonLabel: {
      fontFamily: colors.fonts.sora.extraBold,
      color: colors.textMuted,
      fontSize: 11,
      letterSpacing: 0.7,
      marginBottom: 10,
    },
    reasonList: {
      gap: 10,
      marginBottom: 20,
    },
    reasonRow: {
      flexDirection: 'row',
      alignItems: 'center',
      gap: 12,
      borderWidth: 1.5,
      borderColor: colors.isDark ? colors.navy[700] : colors.border,
      borderRadius: 16,
      paddingVertical: 13,
      paddingHorizontal: 14,
      backgroundColor: colors.isDark ? colors.navy[850] : colors.surface,
    },
    reasonRowActive: {
      borderColor: colors.orange[600],
      backgroundColor: colors.isDark ? colors.alpha.orange18 : colors.orange[100],
    },
    radio: {
      width: 22,
      height: 22,
      borderRadius: 11,
      borderWidth: 1.5,
      borderColor: colors.isDark ? colors.navy[500] : colors.border,
      backgroundColor: colors.surface,
      alignItems: 'center',
      justifyContent: 'center',
    },
    radioSelected: {
      borderWidth: 6,
      borderColor: colors.orange[600],
    },
    radioDot: {
      width: 10,
      height: 10,
      borderRadius: 5,
      backgroundColor: colors.surface,
    },
    reasonText: {
      fontFamily: colors.fonts.sora.medium,
      flex: 1,
      color: colors.text,
      fontSize: 14,
      lineHeight: 20,
    },
    reasonTextActive: {
      fontFamily: colors.fonts.sora.bold,
      color: colors.text,
    },
    actions: {
      flexDirection: 'row',
      gap: 10,
    },
    keepBtn: {
      flex: 1,
      minHeight: 52,
      borderRadius: 14,
      borderWidth: 1.5,
      borderColor: colors.isDark ? colors.navy[700] : colors.border,
      alignItems: 'center',
      justifyContent: 'center',
      backgroundColor: colors.isDark ? colors.navy[850] : colors.surface,
    },
    keepText: {
      fontFamily: colors.fonts.sora.extraBold,
      color: colors.text,
      fontSize: 15,
    },
    cancelBtn: {
      flex: 1,
      minHeight: 52,
      borderRadius: 14,
      alignItems: 'center',
      justifyContent: 'center',
      backgroundColor: colors.red[500],
    },
    cancelText: {
      fontFamily: colors.fonts.sora.extraBold,
      color: colors.white,
      fontSize: 15,
    },
  });
}
